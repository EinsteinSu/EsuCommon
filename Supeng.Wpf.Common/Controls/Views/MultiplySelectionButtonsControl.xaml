<UserControl x:Class="Supeng.Wpf.Common.Controls.Views.MultiplySelectionButtonsControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Button Content="全选" Margin="5" Command="{Binding SelectAllCommand}"/>
        <Button Grid.Column="1" Content="反选" Margin="5" Command="{Binding RevertCommand}"/>
        <Button Grid.Column="2" Content="不选" Margin="5" Command="{Binding UnSelectCommand}"/>
    </Grid>
</UserControl>
