<Window x:Class="Supeng.Sports.Control.Tests.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MainWindow" Height="350" Width="525">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <ToolBar ItemsSource="{Binding ButtonCollection.Collection}" ItemTemplate="{Binding ButtonCollection.Template}"/>
        <TextBlock FontSize ="68"  Grid.Row="1" Text="{Binding DisplayTime}"/>
        <!--<DataGrid Grid.Row="1" ItemsSource="{Binding ButtonCollection.Collection}"/>-->
    </Grid>
</Window>
