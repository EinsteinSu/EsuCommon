<Window x:Class="Supeng.Wpf.Common.Tests.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MainWindow" Height="350" Width="525">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition />
        </Grid.RowDefinitions>
        <ToolBar ItemsSource="{Binding Collection}" ItemTemplate="{Binding Template}" />
        <DataGrid Grid.Row="1" ItemsSource="{Binding Collection}" />
    </Grid>
</Window>